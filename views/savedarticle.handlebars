
<div class="container">
  <h1><Strong>Saved Articles</Strong></h1>
  
</div>

{{#each Saved}}
<div class="container">
    <button  type="button" class="delete btn btn-info">Delete</button>
    <button  type="button"  data-id="{{_id}}" class="comment btn btn-info" data-toggle="modal" data-target="#savedModal">Comment</button>
    <div class="jumbotron1">
    <button class="accordion"><strong>{{Headline}}</strong></button>
      <div class="panel">
        <h6><strong>Summary:</strong> {{Summary}}</h6>
        <h6><strong>URL:</strong> vietcetera.com/en{{URL}}</h6>
      </div>
    </div> 
</div>
 {{/each}}


<!-- Modal -->
<div class="modal fade" id="savedModal" tabindex="-1" role="dialog" aria-labelledby="exampleModalLabel" aria-hidden="true">
  <div class="modal-dialog">
    <div class="modal-content">
      <div class="modal-header">
        <h5 class="modal-title" id="articleId">Modal title</h5>
        <br>
       
      </div>
      <div class="modal-body">
       <textarea id="addComment" class="form-control"></textarea>
        <button type="button" class="close" data-dismiss="modal" aria-label="Close">
          <span aria-hidden="true">&times;</span>
        </button>
      </div>
      <div class="modal-footer">
        <button type="button" class="btn btn-secondary" data-dismiss="modal">Close</button>
        <button type="button" class="savedChanges btn btn-primary">Save changes</button>
      </div>
    </div>
  </div>
</div>


{{!-- Display saved articles here --}}


{{!-- Accordion CSS --}}
 <style>
.accordion {
  background-color: #eee;
  color: #444;
  cursor: pointer;
  padding: 18px;
  width: 50%;
  border: none;
  border-radius: 8px;
  text-align: left;
  outline: none;
  font-size: 20px;
  transition: 0.4s;
}

.active, .accordion:hover {
  background-color: #ccc; 
}

.panel {
  padding: 0 18px;
  display: none;
  background-color: white;
  overflow: hidden;
}
</style>

{{!-- Accordion Javascript --}}
<script>
var acc = document.getElementsByClassName("accordion");
var i;

for (i = 0; i < acc.length; i++) {
  acc[i].addEventListener("click", function() {
    this.classList.toggle("active");
    var panel = this.nextElementSibling;
    if (panel.style.display === "block") {
      panel.style.display = "none";
    } else {
      panel.style.display = "block";
    }
  });
}
</script>
